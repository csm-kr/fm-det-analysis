node_modules/
.next/
out/
next-env.d.ts
tsconfig.tsbuildinfo

# phase execution outputs
phases/**/phase*-output.json
phases/**/step*-output.json

# phase 동시 실행 방지용 파일락 (실행 중에만 존재)
phases/**/.lock

# 로컬 전용 Claude 설정
.claude/settings.local.json

# 환경변수 (시크릿 포함 가능)
.env
.env.*
!.env.example

# ─── ai-ml 산출물 (CLAUDE.md: 가중치/데이터 추적 금지) ───
/data/       # root-level 데이터셋만. configs/data/ 의 Hydra config 는 추적.
runs/
wandb/
outputs/
.hydra/

# 가중치·체크포인트 (PreToolUse hook 도 함께 차단)
*.pt
*.pth
*.ckpt
*.safetensors

# python 빌드/캐시
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/

# OS / 에디터
.DS_Store
.vscode/
.idea/

# torch/hf cache workaround (I-07)
.cache/
