defaults:
  - data: coco
  - model: diffusiondet
  - loss: diffusion
  - train: baseline
  - _self_

seed: ???
device: cuda
tag: baseline
output_dir: runs/${now:%Y%m%d-%H%MUTC}-${tag}  # 컨테이너 = UTC. KST 보려면 +9h.

hydra:
  run:
    dir: ${output_dir}
  output_subdir: .hydra
